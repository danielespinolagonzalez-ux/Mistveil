# ESTADO del proyecto
> Claude Code: actualiza este archivo al final de CADA sesión.

**Base actual:** kit HTML+JS de Daniel (reinicio 2026-07-17). El proyecto Godot anterior (Fases 0–3) vive solo en el historial de git.
**Fase actual:** Auditoría UI/UX COMPLETA — arreglados 11 solapes/desbordes de texto en todas las pantallas (queja de Daniel: "texto descuadrado o que se solapa"). Antes: G5/G7 (dinamismo). Siguiente: feedback de Daniel; luego R3 (contenido de pisos 4–9) o R2.5 (selector de sello).

## Qué hay implementado (resumen del mapeo, 4 informes en paralelo)
Juego jugable de punta a punta: Torre procedural vertical de 9 pisos con 3 biomas y semilla por run; Cadencia completa (compases, counter/parry, coyote, decay); parry activo; Ignición; 3 Artes de Tinta; El Redoble (combate por turnos CTB con QTE); Esfera del Reloj (31 nodos) + XP por desempeño; Santuario (8 nodos por Memoria); pueblo con 7 NPCs, contratos y bendiciones; tutorial de 7 fases; cámaras especiales (fundición, fusión, metrónomo, apuestas); minimapa; guardado localStorage schema 2; audio 100 % procedural con música generativa; 3 esquemas de input + mando + Una Mano móvil; smoke test de ~33 asserts (incluye 60 torres generadas).

## Huecos reales detectados (origen de la Fase R0/R1)
`orbit_shoot` sin código en tiempo real; `escalado_dano_por_piso` huérfano; EventBus sin `off` (sondas fugan); switch duplicado en `esferaBonos`; `buildFloor` (Isaac) desconectado; 6 items en cola `pendientes` (familiares, activo, reveal_map, damage_trail, extra_choice); `data/sellos.json` cargado pero sin código; jefes con patrones decorativos; victoria provisional; fuente por CDN.

## Registro de sesiones
| Fecha | Qué se hizo | Decisiones | Siguiente paso |
|---|---|---|---|
| 2026-07-17 | Reinicio desde el kit HTML+JS de Daniel (commit 8e49d38); verificado: 33/33 PASS y arranque en Chromium. Mapeo exhaustivo con 4 agentes; redactados CLAUDE.md, ROADMAP.md y ESTADO.md nuevos | Motor: vanilla JS sin build; la Torre vertical es el modo canónico; plan en fases R0–R5 (afinación → items → sellos → contenido → refactor → lanzamiento) | Daniel revisa el plan; después, tarea R0.1 |
| 2026-07-17 | Fase R0 completa: orbit_shoot real (polilla orbita+dispara, orbita_px en JSON), escalado de daño por piso vía danoMult, EventBus.off/once, esferaBonos deduplicado, buildFloor retirado; smoke 44/44 PASS. R0.7: build de un solo archivo y publicación como Artifact para que Daniel pruebe con URL | El bundle no toca el código fuente: reescribe imports a un registro por clausuras y embebe JSON+fuente; móvil autodetectado (equivale a movil.html) | Daniel juega la build; después R1.1 (familiares) |
| 2026-07-17 | Fase R1 completa: familiares (gota + polilla orbital), objetos activos con tecla X/botón táctil y recarga por salas (arena del tiempo = slow-mo del mundo), revelar mapa, rastro de aceite, doble pedestal; pendientes=∅ con assert; smoke 54/54. R3.0 adelantada: partículas ambientales por bioma. Hook __mistveil.item(id). Artifact republicado | El activo no tiene gesto en Una Mano (aparcado); tick del rastro a 0.5 s por el redondeo mín. 1 | Daniel: probar R1, elegir dirección gráfica y decidir diseño de Sellos (R2.1) |
| 2026-07-17 | R2 núcleo: sellos.js (colección+equipado persistentes, efectos máquina en sellos.json), Ascuas (dash ardiente + onda de fuego) y Marea (cura por SP + ola lenta) como botín de los jefes de Péndulos/Archivo; estado lento en Enemy; rombo en HUD. Bundle: botón ⛶ pantalla completa + guía iOS. Smoke 59/59; artifact republicado | Sellos = botín (decisión Daniel); iPhone sin API fullscreen → vía "Añadir a pantalla de inicio" | R2.5 (selector) o R3 según Daniel; falta su elección gráfica |
| 2026-07-17 | Dirección de arte aprobada por Daniel ("mágico, cálido, claro"): A1 grade Hora Dorada + A5 Ignición 3 actos; A2 biomas con grade/bruma/tints/atrezzo propios (péndulos colgantes, estanterías goteando, engranajes flotantes+reloj derretido); A3 halos por familia (campo familia en enemigos.json), anillo de telegrafía en windup, auras de élite/jefe | Hooks nuevos: __mistveil.sp(n) y .piso(n); bruma se rehornea al cambiar de bioma; sellos=botín decidido antes | A4 (VFX por habilidad) tras feedback; ajustar tonos con balance.arte/biomas.json vía F5 |
| 2026-07-17 | A4 pase 1: firmas visuales por habilidad (js/artes_fx.js) — nova=corona de tinta índigo, marea=medialuna de espuma turquesa, ascua=abanico de rescoldos que caen, parry=campana de cristal, Cadencia perfecta=numeral romano dorado que salta, dash=estela de engranajes espectrales. Smoke 62/62; artifact republicado | Efectos en world.artFx (spawn/update/draw); dibujados en la capa de mundo tras shockwaves | Falta feedback de Daniel sobre tonos; luego R3 (contenido de pisos) o A2/A3 pase 2 |
| 2026-07-17 | Mejoras de game feel (combate dinámico): G1 dash esquiva sin romper la Cadencia (anillo se congela + rango ensanchado + aviso cian punteado), G2 encadenar combo al siguiente enemigo si el objetivo muere, G3 perfectos recortan cooldown del dash + más libertad de movimiento en combo. Smoke 68/68; artifact republicado | Todo tunable en balance.cadencia; el dash ya no hace _end('dash') | Feedback de Daniel; G4 (i-frames en lunge / bullet-time) opcional; luego R3 |
| 2026-07-17 | G4: i-frames breves al conectar un golpe de Cadencia (perfect 0.22s > good 0.14s, fallar no da nada) — atacar esquiva; halo dorado de aviso; comboIframeT en el getter invulnerable. Smoke 71/71; artifact republicado | Bullet-time por-los-pelos queda como G5 aparcada; i-frames tunables en balance.cadencia | Feedback de Daniel; luego R3 (contenido) o R2.5 (selector de sello) |
| 2026-07-17 | G6 dificultad temprana más suave (Daniel: "cuesta progresar"): presupuesto sala 6→4, incremento/piso data-driven 2→2.25 (piso 9 ≈ igual), escalado HP 0.35→0.30, XP/piso 40→55, nivel_base 80→65. Smoke 75/75; artifact republicado | Curva pensada para aliviar pisos 1-3 sin trivializar el 9; todo en balance.run/xp (F5). Ideas de dinamismo G5/G7 aprobadas y aparcadas | Daniel prueba el nuevo balance; luego R3 (contenido de pisos) |
| 2026-07-17 | G5 bullet-time por-los-pelos (slow-mo azul al rozar bala en dash), G7a dash reposiciona objetivo del combo, G7b disparar durante el combo, G7c barra de Groove/racha (buff dano+velocidad al umbral, cae al recibir; barra HUD + aura). Auditoría UI/UX lanzada en subagente (recorre todas las pantallas buscando solapes). Smoke 82/82 | Confirmado a ojo: la barra superior de controles SE SOLAPA con SP/oro/compás (a arreglar tras la auditoría). Hooks __mistveil.groove(n) | Recibir auditoría UI y arreglar solapes; luego R3 |
| 2026-07-18 | Dirección artística: Daniel comparte un render objetivo (HD-2D cálido con marcos dorados) y elige "empujar el estilo por código, SIN imágenes". Paso 1: chrome ornamentado del HUD por código — helpers `ornateFrame`/`hudBanner`; banner dorado del compás, cartela de piso ("Piso N · …") y marco dorado de la barra SP; barra plana superior → velo en degradado. Smoke 82/82; bundle+artifact actualizados | Se mantiene la regla de cero assets: todo el "premium" es canvas procedural. No se enmarca el grupo de corazones (ancho variable según vida → riesgo de solape); pendiente afinar con Daniel | Reacción de Daniel; luego botones táctiles circulares, emblema del suelo, y más luz cálida — o R3 |
| 2026-07-18 | Corrección de Daniel: "no hay límite de 8 MB". Cierto — era cautela mía excesiva; itch.io acepta juegos web grandes. Reencuadre: lo que importa de verdad es (1) tiempo de 1ª carga en móvil, (2) RAM, (3) que "todo en un archivo" no escala. Soluciones REALES sin límite artificial: fondos en JPEG (opacos, sin alfa → ~1/5 del peso, siempre mejor; fondo_pueblo 1.34MB→135KB, bundle 2.0→1.65MB) implementado en el pipeline (slicer/sprites.js/bundle con campo ext); y carga diferida por bioma cuando el arte crezca (nueva R5.7). Docs y ROADMAP reencuadrados. Smoke 82/82 | No hay tope de peso: la ambición de arte manda; el JPEG y el lazy-load son solo para que arranque ágil | Ejecutar el replanteo: R4.0 escenas.js (prerequisito de escenas) + empezar a cerrar la Torre (lote B, necesita conector) |
| 2026-07-18 | AUDITORÍA DE DISEÑO con la lente del estudio de IA (`docs/auditoria_diseno.md`): clasifico cada pilar en ✅ alineado / ⚠️ replantear / 🆕 oportunidad. Replanteos con más retorno: (1) sacar `js/escenas.js` ANTES de pintar más (evitar bugs latentes del god-file), (2) El Redoble como escena pintada, (3) narrativa barata (viñetas de bioma + final pintado). Jefes = piezas de estado (no frames). Biblia de estilo + presupuesto de fondos (<8MB bundle). ROADMAP actualizado (R3.2/R3.4/R3.5/R3.6, R4.0 adelantada, AS-N1..N5) — todo aditivo, nada borrado | La mayoría del diseño ya estaba alineado (corregido sobre la marcha). 3 bifurcaciones abiertas para Daniel: ambición de fondos, cuánta narrativa, orden del arte restante | Daniel decide las 3 bifurcaciones; luego ejecutar el replanteo (R4.0 primero) |
| 2026-07-18 | PUEBLO ESTILO FFIX (idea de Daniel: escenario pintado + personajes encima): fondo_pueblo generado con Recraft (plaza de relojeros al atardecer) dibujado a PANTALLA COMPLETA con parallax sutil; cámara del pueblo bajada (-80) para que Pip y los 7 NPCs caminen sobre el adoquinado inferior de la pintura; NPCs recolocados por zonas. Fondo fijo (no world-anchored) para evitar franjas vacías. BONUS: cazado un bug real en input.js (una_mano: el tap usaba x/y indefinidos → ReferenceError en cada toque; ahora st.lx/st.ly). Verificado en escritorio Y móvil. Smoke 82/82. Hook __mistveil.pos() para ajuste de layout | El fondo tiene 55% casas / 45% adoquín: se recorta arriba y se rellena el lienzo; los pnj pintados de la lámina hacen de vecinos de fondo | Daniel valida el pueblo; lote B de enemigos o biomas 2-3 |
| 2026-07-18 | Proporciones del pueblo decididas por Claude (Daniel delega: "yo no las veo bien"): altura visual por IDENTIDAD en vez de por radio de colisión — Pip 34 → Margo 42, Vesper 46, Hermanos 38, tablones 42/46, pabellón de El Redoble 78 y Puerta del Reloj 94 (monumental); estructuras grandes quietas y con sombra acorde al ancho, personajes respiran. Enemigos de la Torre sin tocar (ya escalan por radio). Smoke 82/82 | Tabla ALTURAS en drawNPC (pueblo.js): ajustar un tamaño = un número | Daniel pasea; lote B de enemigos o biomas 2-3 |
| 2026-07-18 | PUEBLO PINTADO (tanda ~40 créditos, quedan ~925): Margo (regenerada de cuerpo entero), Lady Vesper, Hermanos Nº7 y Nº12 (gorros picudos muy FFIX), Tablón, Tablón del Gremio, pabellón de El Redoble y Puerta del Reloj entreabierta con luz; retratos de diálogo estilo FFIX (Margo, Vesper volteada hacia el texto, Hermanos y PIP vía i2i desde su propio sprite). Caja de diálogo: retrato del QUE HABLA (las líneas de Pip muestran a Pip), 3 líneas medidas, caja a 78px. Hook __mistveil.hablar(id). Smoke 82/82 | El rótulo "GUILD" del gremio queda ilegible a 64px (aceptado); los sujetos "detrás de mostrador" tienden a salir cortados: pedir FULL BODY head to feet (anotado) | Daniel pasea por el pueblo en el iPhone; siguiente: lote B de enemigos o biomas 2-3 |
| 2026-07-18 | PROPS DE LA REFERENCIA (tanda ~10 créditos, quedan ~980): emblema rosa-estrella del suelo (i2i desde el recorte del mockup de Daniel, estampado con máscara radial en salas inicial/jefe — caldeo rojizo en jefe), estandartes púrpura con esfera de reloj bordada flanqueando la puerta norte (variedad por semilla de sala) y candelabros de bronce de pared (la llama procedural parpadea encima). Todo con fallback vectorial. Smoke 82/82 | El grabado vectorial del reloj queda como fallback; los estandartes no salen en todas las salas (hash>0.3) para no cansar | Daniel valida en iPhone; siguiente: biomas 2-3 con este método o lote B de enemigos |
| 2026-07-18 | ENTORNOS + UI/UX pintados (tanda ~12 créditos, quedan ~1000): texturas del bioma Péndulos (suelo de losas ámbar con engranajes + muro de sillería) integradas como patrón en el _bake con achatado 2.5D, veladuras y fallback procedural; botones táctiles = medallones de oro forjado (hoja 2×3 multi-recortada + llama suelta: PAR/compás/Arte/Ignición/activo). Troceador gana modos textura y hoja; Sprites.load espera decodificación (el bake ve las texturas). El muro necesitó 2 intentos (el 1º salió "muralla con paisaje": encuadre macro forzado en el prompt, patrón anotado) | Las texturas llegan luminosas: veladura oscura encima en el bake, no en el asset (ajustable por código). Cartas y frasco de la hoja quedan sin uso (reserva) | Daniel prueba entorno+medallones en iPhone; siguiente: texturas Archivo/Invertida o lote B de enemigos |
| 2026-07-18 | LOTE A de arte producido por Recraft MCP (tanda de ~13 créditos, quedan ~1014): tejedor de horas (araña-reloj de latón), campanero (guerrero-campana de bronce) y velón inestable (monstruo de cera agrietada) — generados n=2 con la plantilla del catálogo, elegidos, remove_background de Recraft, troceados e integrados; los 4 enemigos del piso 1 ya van pintados. create_style sigue 500 (2º día — bug persistente de Recraft, AS4a pendiente). El velón necesitó regeneración anti-"peluche" (negativas más duras: quedó anotado el patrón) | Los sujetos "criatura entrañable" tienden a lo mono: reforzar con "menacing/grotesque/NO fur" desde el prompt inicial en los que aplique | Daniel bendice o veta el lote A; luego lote B (resto de enemigos + pip_dash + Tuerca) |
| 2026-07-18 | Pipeline Recraft SISTEMATIZADO (petición Daniel: "optimizar en función de cómo procese Recraft"): `tools/encargo.json` = catálogo machine-readable de ~55 assets con la fórmula de prompt validada (inglés + ancla CRITICAL STYLE + fondo violeta oscuro), targetH/face/largestOnly por asset y plan de lotes A→I (~250 créditos); el troceador lee su config de ahí (una sola fuente de verdad); `docs/pipeline_recraft.md` = proceso operativo con lo aprendido (remove_background de Recraft siempre, WebP disfrazado, n=2, reintentar create_style y control A/B) | Prompts de producción en inglés (Recraft los sigue mejor, validado); encargo_arte.md queda como doc de diseño | Daniel reactiva el conector Recraft; lote A (tejedor, campanero, velón) |
| 2026-07-18 | PRIMER ARTE PINTADO EN EL JUEGO: Daniel entrega 3 imágenes de Pip (idle/paso/ataque); AS1 troceador `tools/slice_assets.mjs` (detecta alfa real / damero pintado de ChatGPT / fondo degradado; recorte+reducción a 2×; red de seguridad si el algoritmo "se come" al sujeto) y AS2 cargador `js/sprites.js` con fallback por-código; `drawPlayer` usa el sprite (pose por estado, volteo por dirección de apuntado, hereda embestida/squash/bob del motor; estelas, auras y arco de golpe siguen procedurales); bundle incrusta los PNG en base64 (+27 KB). Smoke 82/82; artifact republicado | Los originales de Daniel NO van a git (assets/src/ ignorado; pesarían ~100 MB al final) — al repo solo los troceados. Sprites a 2× y dibujados a la mitad (nitidez) | Daniel genera el resto del lote de validación (3 enemigos + suelo/muro Péndulos + iconos); luego AS3b enemigos en drawEnemy |
| 2026-07-18 | GIRO DE ARTE (decisión Daniel): entra arte pintado generado por él con ChatGPT — estilo romanticismo + FFIX/FFXIV. Redactado `docs/encargo_arte.md`: ~55 assets inventariados desde los JSON reales (10 enemigos, 3 jefes, 8 NPCs+8 retratos, 3 biomas, 20 reliquias, 8 sellos, UI, portada) con prompts exactos, llave de estilo adjuntable, bloque técnico común (fondo transparente, vista 3/4 cenital, sin sombra) y hojas de iconos en rejilla. Nueva fase R-Assets en ROADMAP (AS0 hecha) | Sin hojas de animación (ChatGPT no es consistente entre fotogramas; el motor ya anima proceduralmente sobre sprite estático). Troceado futuro por transparencia con Chromium headless (cero deps nuevas). Integración SIEMPRE con fallback al dibujo por código | Daniel genera el lote de validación (8 imágenes); Claude monta AS1+AS2 (troceador + cargador) |
| 2026-07-18 | Daniel: "realmente no" al primer pase — recomposición a fondo del HUD para que todo quede cuadrado: banner del compás CENTRADO arriba (como su referencia) y cartela de piso a la dcha, ambos a la misma altura (y=3, h=15); fila del oro en FLUJO medido (oro→sello→objeto activo; antes x fijas que se pisaban con oro de 2-3 cifras); caja del Arte y reliquias movidas a x≥152 (el ¡F! del SP ya no choca con ellas); ¡RACHA! bajo su propia barra; minimapa bajo la cartela y, en táctil, apartado de los botones pausa/≡ (la Torre vertical lo hacía crecer debajo de ellos); botón ⛶ del bundle abajo-izda (tapaba corazones). ornateFrame v2: fondo casi opaco, doble borde, puntas laterales y joya. Verificado con carga máxima (sello+activo+SP lleno+racha+4 reliquias) en escritorio y táctil. Smoke 82/82 | El HUD ya no usa posiciones fijas en la fila del oro: todo se mide (measureText) y fluye — robusto ante números grandes | Reacción de Daniel al nuevo orden; luego botones táctiles vestidos o R3 |
| 2026-07-18 | Auditoría UI/UX resuelta (11 arreglos, verificados con capturas headless): (1) barra de controles movida abajo y temporizada + etiqueta de piso corta — se acabó el solape superior; (2) cabecera del menú mide y separa engranajes/nivel/oro; (3) Crónica: columnas de valores separadas y nombres recortados con "…"; (4) menú de El Redoble a 14px; (5) balance con más aire; (6) subtítulo del tutorial se ajusta a varias líneas; (7) título de victoria auto-encoge para caber; (8) tarjetas de mejora sin desbordar + HUD ya no "sangra" bajo overlays; (9) sin el numeral 'VI' tras el panel de la Esfera; (10) contrato en 2 líneas; (11) emojis 🔒/🍞/⛓ (tofu) dibujados. Ayudas font medido (fitFont/wrapText/clipText). Smoke 82/82; bundle rebuild | font(size) renderiza a size*2 px: los layouts contaban con el tamaño sin doblar y por eso desbordaban. HUD/tutorial/táctil solo en modo 'play' (evita el sangrado). Todo medido con measureText: robusto ante valores largos | Daniel prueba en el móvil; luego R3 (contenido pisos 4-9) o R2.5 (selector de sello) |
| 2026-07-18 | LOTE B de enemigos por Recraft MCP (tanda de 30 créditos, quedan 830/860): Cerero Errante (figura de cera encapuchada vertiendo cera ardiente), Engranaje Errante (engranaje de latón con ojo mecánico), Cuco de Cuerda y Polilla del Polvo con **prompt de amenaza** cargado (eerie/menacing/unsettling — anti-"peluche" del estudio de IA), Minutero Gemelo (esgrimista de dos agujas) y El Afinador (figura de polvo con diapasón). Flujo por asset: generate_image n=2 1:1 con `plantilla_sprite`+sujeto → elegir → remove_background de Recraft → assets/src → `tools/slice_assets.mjs` (troceados a targetH). Integración CERO código: `drawEnemy` recoge `enemigo_<id>` solo. Los 10 enemigos de la Torre ya van pintados. `estado:"hecho"` en encargo.json. Verificado: node --check, smoke 82/82, y captura headless en-motor (testCad(false)+spawn de los 6, step ok, Sprites.get de los 6 OK). Bundle+artifact republicados | La amenaza desde el prompt inicial evita la "deriva a lo mono" en cuco/polilla (debilidad #4 del estudio); spawn(id,1) apila en un punto → la lámina comparativa se compone con el loader real | Daniel bendice/veta el lote B; luego lote C (3 jefes) o R3 contenido |
| 2026-07-18 | PIP FLUIDO — set COMPLETO de 8 poses regeneradas con máxima consistencia desde una nueva referencia de Daniel (Pip de latón, capa índigo, llave de cuerda) vía **i2i de Recraft**: idle, andar A, andar B (ciclo real de 2 fotogramas), ataque, dash, arte (lanzar hechizo), ignición (overdrive) y parada. `create_style` sigue devolviendo 500 (3ª sesión — bug persistente) → plan B i2i desde la referencia, validado antes de gastar (strength 0.4→0.6: la identidad de Pip aguanta incluso en poses grandes). `drawPlayer` cableado a cada estado con cadena de prioridad (dash>ataque>parada>arte>ignición>andar>idle); nuevas ventanas visuales `castT`/`parryAnimT` en Player (set en 'hechizo_lanzado' y en la parada); tabla `POSE_DRAWH` por pose para que el CUERPO de Pip mantenga el mismo tamaño pese a capas/efectos que se extienden (la escala la fija el motor, no la IA). Verificado: node --check, smoke 84/84 (2 asserts nuevos), lámina comparativa de las 8 + ignición capturada EN-MOTOR. Tanda ~40 créditos (quedan 790). Bundle+artifact republicados | i2i-desde-referencia = palanca de consistencia mientras `create_style` siga caído (regla del estudio: variantes de un personaje SIEMPRE desde su imagen). Cero sprite-sheets: 8 poses clave + el motor anima (bob/embestida/squash/ciclo/estelas). POSE_DRAWH/POSE_DY afinables a ojo | Daniel juega y valida el feel; ¿regenerar retrato de Pip desde esta referencia? luego lote C (jefes) o R3 |
| 2026-07-18 | AFINADO de Pip (feedback de Daniel): (1) ahora **mira hacia donde CAMINA** (antes miraba al apuntado): `Player.facing` se calcula en update por el signo de vx al moverse, por el ataque al golpear y por el apuntado al estar parado; drawPlayer lo lee. (2) **Transiciones por código**: giro suave izquierda↔derecha (squash horizontal 130 ms, Pip "pivota") y **crossfade** entre poses distintas (75 ms), ambos con performance.now() — NO entre los dos fotogramas de andar (ese cambio ES el paso). Helper `drawPose(pid,alpha)` para dibujar saliente+entrante. Verificado: node --check, smoke 84/84 y captura de zoom (Pip mira ← al ir a la izda, → a la dcha) sin errores. Bundle+artifact republicados | facing con umbral en vx (no voltea al moverse solo en vertical); al parar mira al apuntado (puede voltear al soltar la tecla — avisar a Daniel por si prefiere que mantenga el último lado). FLIP_MS/POSE_FADE_MS afinables | Daniel prueba el giro/feel; ¿retrato de Pip? luego lote C o R3 |
| 2026-07-18 | CONTENIDO NUEVO (encargo "nuevos enemigos, zonas y formas de mapa"; 2 agentes mapearon enemigos y generador antes de tocar el god-file). **Enemigos**: 5 nuevos + 2 comportamientos limpios en entities.js — `charger` (Badajo Errante: telegrafía y EMBISTE en línea recta, se compromete con la dirección) y `spiral_shooter` (Girándula: balas en espiral rotatoria); más Rémora de Tinta (chase, Archivo), Peón de Sebo (wander_bounce, relleno barato) y Címbalo (turret, trueno). Cada uno con fallback vectorial propio en drawEnemy + PALETTES, integrados en los pools por bioma. **Zona nueva**: 4º bioma **La Bóveda de Truenos** (pisos 10-12; la Torre pasa de 9→12 vía `floors_total` — revertible a 9 con un número) con mecánica propia: DESCARGAS telegrafiadas a compás (aro que se cierra → rayo + fogonazo, daño en área) solo en salas de combate; paleta violeta/trueno, niebla/grade, atrezzo (tubos de órgano + campanas), jefe El Gran Carillón y pool propio. **Formas de mapa**: 4 plantillas nuevas (cruz, trinchera con puente, diagonales+cera, arena) que aplican a TODOS los pisos. Verificado: node --check, smoke 95/95 (11 asserts nuevos) y capturas en-motor (5 enemigos + entrada de la Bóveda + rayos). Cero créditos (todo código/data) | La escala de la Torre (12 vs 9) es la única decisión de game feel: revertir = `floors_total` a 9 y el 4º bioma queda inaccesible sin romper nada (biomaDe capa por índice). El campo `biomas.pisos` es cosmético; manda el ORDEN del array. Sprites de los 5 enemigos = tanda de arte aparte (usan fallback vectorial) | Daniel juega el contenido; luego sprites de los 5 enemigos (Recraft) o afinar el balance de la Bóveda/12 pisos |
| 2026-07-18 | ARTE de los 5 enemigos nuevos (tanda Recraft ~30 créditos, quedan ~760): Badajo Errante (badajo de bronce con anilla rota + chispas violeta), Girándula (veleta-molinete de latón con ojo central), Rémora de Tinta (pez de tinta goteando), Peón de Sebo (peoncito de cera con mecha) y Címbalo (platillo de bronce con arcos eléctricos). Flujo estándar: generate_image n=2 1:1 con `plantilla_sprite`+sujeto → elegir → remove_background → assets/src → slice. `drawEnemy` los recoge por `enemigo_<id>` (el fallback vectorial queda de red). Los 15 enemigos de la Torre ya van pintados; `estado:"hecho"` en encargo. Verificado: smoke 95/95, lámina de los 5 + captura en-motor. Bundle 47 sprites; artifact republicado | El fallback vectorial sigue ahí por si falta un PNG (robustez); la Rémora dejó goteos sueltos que `largestOnly` limpió | Daniel juega; luego lote C (jefes) o afinar balance de la Bóveda/12 pisos |
| 2026-07-18 | MECÁNICAS DE ENEMIGO MÁS VARIADAS (encargo de Daniel): 4 patrones nuevos, cada uno con una interacción DISTINTA. `blink` (**Saltahoras**): se TELETRANSPORTA a tu flanco y dispara — reaccionas, no persigues. `shielder` (**Broquel de Bronce**): rebota los golpes FRONTALES; su escudo gira despacio (bloqueo por arco en `takeDamage` con `_faceAng`) → flanquéalo. `divide` (**Cúmulo de Sebo**): al morir se PARTE en 2 peones (hook en `enemy_died`) → gestión de multitud. `healer` (**Restaurador**): huye y CURA a los enemigos heridos cercanos → mátalo primero. Firmas FX (enemy_blink/escudo_bloqueo/enemy_curado), fallback vectorial + PALETTES, integrados en los pools por bioma. Verificado: node --check, smoke 103/103 (8 asserts nuevos: existencia + teletransporte + frente<espalda + cría válida + cura de aliado) y captura en-motor (comportamientos + división sin errores). Cero créditos (código/data) | Ninguna toca la física del jugador (no empujan ni invierten controles): son patrones de IA nuevos, seguros. El bloqueo del broquel deja chip mínimo de 1 por el redondeo. Sprites de los 4 = tanda de arte aparte (van con fallback vectorial) | Daniel juega; luego sprites de los 4 + lote C (jefes) |
| 2026-07-18 | JEFES DE RAID estilo FFXIV — **VERTICAL SLICE** (motor + jefe de Péndulos; decisión Daniel: jefes distintos por bioma, por fases, con mecánicas telegrafiadas que esquivas). Nuevo módulo **`js/jefes.js`**: un director ANCLA al jefe y lanza MECÁNICAS telegrafiadas — `circulos` (charcos, uno sobre ti), `donut` ("acércate": peligro fuera de un radio), `cono` (cleave), `linea`, `barrido` (manecilla que gira y barre), `safe` (zona segura) y `adds` (invocar). FASES por umbral de vida cambian el set. La LÓGICA (timers/resolución/daño) vive en jefes.js; main.js dibuja `director.zonas` (aros que se cierran) + barra de vida grande con nombre y fase. Data en **`data/jefes.json`** (jefe por bioma + biblioteca de mecánicas). Integración: `data_db` carga jefes.json (14º archivo); `comportamiento:'jefe_ancla'` (branch de jefe LEGACY gateado); `rooms.enter` spawnea el jefe raid con fallback a `jefes_bioma`; sello/nombre leen jefes.json. Jefe "El Gran Campanero" (Péndulos), 2 fases. Hook `__mistveil.jefe(bioma)`. Verificado: node --check, smoke 107/107 (4 asserts) y capturas en-motor (conos + charcos + barra de fase) + bundle autocontenido arranca | Los jefes ya NO se repiten entre escenarios: cada bioma tendrá el suyo (por ahora solo Péndulos usa el motor; Archivo/Invertida/Truenos caen al legacy hasta definirlos — casi solo datos). Jefe cuasi-estático (FFXIV): las mecánicas son la amenaza, no su cuerpo | Daniel valida el FEEL de las mecánicas; si va bien: definir los otros 3 jefes en jefes.json + más tipos de mecánica + arte de jefe |
| 2026-07-18 | LOS 4 JEFES DISTINTOS (Daniel: "define los otros 3"). Cada bioma tiene ya su jefe con **base/sprite propio** (nada de repetir), paleta de telegrafía y set de mecánicas temático, todo en `data/jefes.json` (SIN código nuevo — el motor lo soporta): **El Gran Campanero** (Péndulos, campana, ámbar/oro), **La Archivera Anegada** (Archivo, polilla, índigo; charcos/página/remolino/marea segura/invoca rémoras), **El Primer Relojero** (Invertida, araña-reloj, cobre/rojo; manecillas que barren ×2, números romanos, cono horario, engranaje-donut), **El Gran Carillón** (Truenos, cuco, violeta; descargas, resonancia, tañido, arco eléctrico, invoca címbalos). Los `adds` reutilizan enemigos nuevos (velón/rémora/címbalo). 21 mecánicas en la biblioteca. Verificado: smoke 116/116 (9 asserts de jefe, incl. "los 4 NO repiten base") y captura en-motor de los 4 (sprite+bioma+barra de fase distintos) sin errores; bundle 1860 KB | Roba-compás de la Invertida legacy no se porta al raid (era del branch de campanada gateado); reimplementable como mecánica. El escalado de HP por piso puede dejar a los jefes tardíos muy tochos → afinar jugando | Daniel juega los 4 jefes; luego arte de jefe dedicado, más tipos de mecánica (empuje/torres/tethers) o roba-compás |
| 2026-07-19 | ARTE DE JEFE DEDICADO (encargo "crear todo el arte que falta"): sprites propios pintados para los 4 jefes de raid (Recraft, ~24 créditos, quedan ~717) — **El Gran Campanero** (campana ceremonial de bronce con corona de velas y brazos forjados), **La Archivera Anegada** (polilla-bibliotecaria empapada en tinta índigo, alas como páginas que se deshacen, prompt de amenaza), **El Primer Relojero** (autómata roto y solemne, pecho abierto con engranajes de oro, báculo-manecilla) y **El Gran Carillón** (torre-campanario con muchas campanas y arcos de rayo, prompt de amenaza). Hook de código: `drawEnemy` elige `e.def.sprite` (=`jefe_<bioma>`) si existe, si no cae al sprite del enemigo base; los jefes se dibujan **más grandes** (dh ∝ radio, resto conserva 32px); `rooms.enter` y `__mistveil.jefe` inyectan `sprite:'jefe_'+bioma.id`. Flujo estándar: generate_image n=2 1:1 con `plantilla_sprite`(clase boss)+sujeto → elegir → remove_background → assets/src → slice (96–100px). `estado:"hecho"` en encargo. Verificado: node --check ×4, smoke 116/116 y captura en-motor de los 4 (sprite dedicado + su bioma + barra de fase). Bundle 61 sprites; artifact republicado | Los ids de encargo se renombraron a `jefe_<bioma>` (pendulos/archivo/invertida/truenos) para casar con la convención del hook; retrocompatible (si falta el PNG, cae al sprite base). Añadido `jefe_truenos` que no existía en el catálogo | Daniel valida los jefes pintados; siguiente tanda de arte: NPCs+retratos que faltan, props o hojas de iconos |
| 2026-07-19 | PORTADA + MASCOTA pintadas (arte que falta, ~8 créditos, quedan ~709). **portada_titulo**: la Torre de Mistveil alzándose entre niebla dorada al atardecer con Pip diminuto de espaldas ante la puerta (composición "figura pequeña ante lo sublime"); `drawTitle` la pinta a pantalla completa (cover) con veladura vertical para que MISTVEIL + el prompt se lean encima; el péndulo/esfera procedurales quedan de FALLBACK si falta la lámina. **tuerca_mascota**: el engranajito con ojos y patas del Nv.3; `drawFamiliar`/render de `world.pet` usa el sprite (hereda el bob) con el dibujo vectorial de red. La portada entra como `textura` jpeg maxDim 1024 (como `fondo_pueblo`). AUDITORÍA DEL CATÁLOGO: los 4 `npc_*` pendientes (ferran/templanza/virtuoso/eco_bronce) NO son personajes reales — templanza/virtuoso/eco son **contratos del gremio** y ferran no existe; `prop_puerta_reloj` lo reemplazó `npc_puerta`. Marcados `descartado` en encargo (no generar arte huérfano). Verificado: node --check, smoke 116/116, captura en-motor de la portada (impecable) + sprites cargados sin error. Bundle 63 sprites; artifact republicado | Si Daniel quiere de verdad esos 4 personajes (p.ej. un tendero Ferran + personificar los contratos), es una FEATURE con código+UI, no solo arte → decisión suya. La portada respeta el hueco superior para el título; el prompt "Enter/toca" se lee por la veladura | Daniel valida portada y mascota en el iPhone; siguiente: props del entorno o hojas de iconos |
| 2026-07-19 | PROPS DEL ENTORNO pintados (11 props, ~44 créditos, quedan ~665). Un agente Explore mapeó cada punto de dibujo ANTES de tocar el god-file. Helper nuevo `drawPropSprite(id, cx, byY, dh, alpha)` (ancla por la base, devuelve false → fallback vectorial). Cableados en su sitio con red vectorial: **prop_roca** (drawRock), **prop_bloque_cera** (drawWaxBlock, conserva las grietas de daño encima), **prop_pedestal** (drawPedestalObj, la reliquia flotante sigue aparte), **prop_trampilla** (drawTrapdoor, vista cenital, anillo de descenso encima), **prop_mostrador_tienda** (mueble de fondo tras los productos de la tienda). En `drawBiomaDecor` (decorado sutil por bioma) se sustituye el vector por sprite: Péndulos → **prop_pendulo_colgante** (pivota desde el techo) + **prop_candelabro** de pie; Archivo → **prop_estanteria_tinta** + **prop_pila_libros**; Invertida → **prop_engranaje_flotante** (gira) + **prop_reloj_derretido**. Truenos (órgano) queda vectorial (sin prop equivalente). Verificado: node --check, smoke 116/116, captura en-motor de los 3 biomas (estanterías/pila/candelabro/engranajes visibles) + los 11 sprites cargados sin error. Bundle 74 sprites; artifact republicado | Cero código de gameplay nuevo: solo dibujo. El decorado va a ~0.7 de alfa (recede como fondo). rock/wax/pedestal/tienda comparten el mismo patrón helper+fallback que el decorado ya verificado | Daniel pasea los biomas; ya solo faltan las 4 hojas de iconos (reliquias/artes/sellos/UI) |
| 2026-07-19 | HOJAS DE ICONOS pintadas (3 de 4, ~20 créditos, quedan ~645). Un agente Explore mapeó los puntos de dibujo del HUD. **Herramienta**: nuevo modo `grid` en `slice_assets.mjs` (`procesarRejilla`) que recorta la hoja en rows×cols celdas deterministas y recorta cada una por su alfa — robusto a iconos multi-blob (frascos, gotas gemelas) que rompían la detección por componentes. Helper `drawIconCentered(id,cx,cy,dh)`. **hoja_iconos_ui** (2×4): corazones de cera **lleno/medio/vacío** (drawHeart, el gran cambio de identidad — se ven por todo el HUD), moneda-engranaje de oro (hud_oro) y gema de Memoria (HUD del pueblo); gear/gota/reloj quedan de reserva. **hoja_iconos_artes** (1×3): tinta/marea/ascua cableados por `arte_<hechizo>` en el selector de Arte. **hoja_iconos_reliquias** (4×5 = 20): el orden de la hoja CASA con `items.json`, así que cada icono es `icono_<itemId>` y la bandeja de reliquias del HUD (y el fallback) los recoge por id, contenidos en su caja de 11px. Todo con red vectorial. **hoja_iconos_sellos DIFERIDA**: `data/sellos.json` no lo lee ningún sistema aún (sin punto de dibujo) → generar cuando exista el UI de sellos, no arte huérfano. Verificado: node --check ×2, smoke 116/116, captura en-motor del HUD (corazones/moneda/arte/bandeja con 8 reliquias) sin errores. Bundle 105 sprites (4001 KB); artifact republicado. **Con esto el catálogo de arte queda al día**: 56→79 hechos, solo la hoja de sellos diferida (bloqueada por su feature) | El troceador gana un modo reutilizable para cualquier hoja futura. Los iconos de la bandeja van a 11px (diminutos pero legibles y distintos). hud_sp/engranaje/reloj sliced pero sin cablear (reserva, como cartas/frasco de los botones) | Daniel valida el HUD pintado en el iPhone; arte pendiente = solo sellos (espera su UI). Siguiente foco: game feel / R3 contenido / refactor del god-file |
| 2026-07-19 | UI/UX — MÁS PERFILADO + CONTROLES A DOS PULGARES (petición de Daniel). (1) **Fuente Gelica** (serif, sustituye a VT323): incrustada local `assets/fonts/Gelica-Regular.otf` (adiós dependencia de Google Fonts); `font()`, cadencia, index/movil/bundle. Solo vino el peso Regular (faltan los demás — pedidos a Daniel). (2) **Render "perfilado"**: SUPERSAMPLING — el lienzo interno pasa a 2× (`SS=2`, backing 1280×720) con `imageSmoothingEnabled` y `image-rendering:auto`; el texto y el arte pintado salen nítidos y suaves en vez de pixelados. `resize()` ya desacoplaba el CSS del backing, así que el display no cambia. `render()` fija la escala base cada frame. (3) **Fix de coords táctiles**: como el backing ya no es VW×VH, `input.js` mapea los toques a coords LÓGICAS (`Input.init(canvas, toWorld, VW, VH)`; `rawPos`/mitades usan `LW/LH`) — si no, los botones no casaban. (4) **Combate al CENTRO-ABAJO**: Ignición (centro, la mayor), Parada y Arte flanqueándola + objeto activo; alcanzables con los dos pulgares desde los sticks. Sistema (pausa/≡/♪) agrupado arriba dcha. (5) **Feedback de pulsación**: nuevo sfx `ui_tap` (tick suave) al tocar cualquier botón + resplandor radial y onda reforzados en `drawTouchUI` (la vibración `vib(9)` ya estaba). Verificado: node --check ×3, smoke 116/116, capturas escritorio (título/HUD nítidos) y móvil (cluster inferior; un tap central DISPARÓ Ignición → el mapeo lógico funciona) + arranque del bundle (canvas.width=1280, Gelica ok) sin errores | Supersampling = 4× fill-rate: si el móvil de Daniel va justo, bajar `SS` a 1.5. Gelica es serif de display: a 8px (font(6)) es legible pero fino — vigilar en el iPhone. Los comentarios viejos que citan "VT323" quedan como nota histórica | Daniel prueba el iPhone (¿fluidez con SS=2? ¿legibilidad de Gelica pequeña?) y me pasa el resto de pesos de Gelica; luego el doc de audio |
| 2026-07-19 | FIX — las áreas de daño del jefe "seguían" al jugador (bug de coords en móvil). Causa: `drawBiomaExtras` y `drawBossZonas` se llamaban desde `drawHUD` (espacio de PANTALLA, sin zoom) usando `SX/SY`, que restan la cámara pero NO aplican `ZOOM`. En escritorio `ZOOM=1` → coincidía; en móvil `ZOOM=1.45` → las telegrafías salían a otra escala y se desplazaban a distinto ritmo que el suelo, pareciendo "pegadas" al jugador. Arreglo: envolver esos efectos de MUNDO en el mismo `translate(shake)+scale(ZOOM)` que el bloque del mundo; la barra de vida del jefe (`drawBossHP`) y el minimapa se quedan en pantalla. De regalo, también corrige en móvil los peligros de bioma (péndulos/rayos), altares y la mascota, que tenían el mismo desfase latente. Verificado: node --check, smoke 116/116 y capturas en-motor a `ZOOM=1.45` (círculos sobre el suelo, manecilla que barre desde el jefe — todo anclado al mundo) y escritorio sin regresión (usé un hook temporal `__heal` para sobrevivir a las telegrafías y lo REVERTÍ antes del commit) | La lógica de `jefes.js` ya guardaba coords de mundo fijas (no era ahí el bug); era puro render. `SX/SY` = pre-zoom a propósito (el `scale(ZOOM)` del bloque de mundo lo aplica) — cualquier dibujo de mundo hecho fuera de ese bloque necesita su propio `scale(ZOOM)` | Daniel prueba los jefes en el iPhone: ¿las áreas cuadran ya con el daño real? Si quiere más claridad, subo el contraste de la telegrafía |
| 2026-07-19 | MÚSICA GENERADA + INTEGRADA (Daniel: "todas geniales, encuéntrales cabida y enlázalas; por separado ya va bien"). 13 pistas creadas con el conector de audio de la sesión (motor **ElevenLabs music v2**, instrumental, estilo PS1-JRPG relojería sin nombres propios por ToS): título, pueblo, péndulos, archivo, invertida, truenos, jefe, El Redoble, santuario, victoria, derrota + 2 stingers (ignición/piso, generados a 10s y recortados a ~3s con ffmpeg vía imageio-ffmpeg). Guardadas en **`assets/audio/*.mp3`** (~9,7 MB, servidas aparte, NO embebidas — opción C). **AudioManager** gana reproductor real: `playTrack/crossfadeTo` (HTMLAudio con fundido y ducking del dron procedural), `sample` (one-shots) y `unlockMusic` (autoplay tras el gesto del `kick`); si falta el fichero o el autoplay está bloqueado, se mantiene el **dron procedural** de siempre. **Director** `musicaEscena()` en main.js (en `update`): title/pueblo/santuario/batalla/dead/victory y en `play` por bioma + `mus_jefe` si hay jefe vivo; stingers en `ignicion_started` y `descendFloor`. Verificado: node --check, smoke 116/116, captura headless con audio desbloqueado (title→péndulos→archivo→invertida→jefe cambian de pista, todas *playing*, 0 errores) y arranque del bundle SIN assets/audio (cae al dron, sin crash) | El **artifact** (host con CSP) no puede cargar `assets/audio/` → se queda con el dron procedural; para oír la música real hay que **servir el juego** (http local / deploy con `assets/audio/`). Ofrecido a Daniel: si quiere oírla en el iPhone por el artifact, embebo el audio (build "con audio", ~+13 MB). Falta `mus_jefe_capa2` (capa de Fase 2) y los SFX de verdad (ElevenLabs). Volumen respeta `vol_musica`/`vol_sfx`; los cambios en caliente de volumen no se aplican a una pista ya sonando (deuda menor) | Daniel escucha la música en contexto (servido) y decide si embeber en el artifact; luego SFX con ElevenLabs y la capa2 del jefe |
| 2026-07-19 | MANDOS MFi/Xbox/PS en iPhone (petición de Daniel; "imfp"=**MFi**, Made for iPhone). Investigado: iOS Safari soporta la **Gamepad API** desde iOS 10.3 (eventos `gamepadconnected` + `navigator.getGamepads()`), con Xbox/PS5/MFi por Bluetooth/Lightning; exige **HTTPS** y una **pulsación** antes de aparecer. El juego YA tenía twin-stick de mando (`pollGamepad`, deadzone radial, auto-calibración de reposo), pero en móvil el modo táctil/`una_mano` cortocircuitaba el apuntado y el disparo del stick, y no había detección ni aviso. Cambios en `input.js`: detección por evento + red de sondeo (`padPresent`/`padJustConnected`), el **stick derecho apunta y dispara SIEMPRE** (también en una_mano), sin caer al ratón cuando hay mando (evita el "mirar al centro" al soltar), y **remapeo estándar** intuitivo (A golpe/confirmar, B dash, X arte, Y ignición, LB parada, RB objeto activo, Start pausa, View menú) + `hideTouch`. En `main.js`: al conectar un mando pasa a twin-stick (`raton`), oculta el overlay táctil y avisa ("Mando conectado…"); volver a tocar reactiva el táctil (lo último manda). Verificado: node --check, smoke 116/116 y prueba headless con **mando sintético** (tras calibrar reposo: left-stick mueve, right-stick apunta+dispara `active:true`, A=melee, overlay táctil oculto, 0 errores) | La auto-calibración toma el 1er sondeo como reposo → si el mando ya viene con el stick torcido, ese sesgo cuenta como centro (raro en la práctica; el stick está neutro al conectar). El **artifact** va en iframe: el mando puede requerir `allow="gamepad"` en el host — funciona seguro en el juego **servido/deploy**. Vibración de mando: la Haptics API es limitada en iOS (no implementada) | Daniel prueba con su mando (Backbone/Kishi/Xbox/PS) en un deploy HTTPS; si algún botón no cae donde espera, ajusto el mapa |
| 2026-07-19 | HOSTING en GitHub Pages (para cargar audio/mando fuera del artifact, que va en iframe con CSP). FTP descartado: el sandbox solo saca HTTPS por proxy (21/22 bloqueados) — no se puede subir por FTP desde aquí, y evitamos credenciales en el chat. GitHub Pages es mejor: URL directa HTTPS (no iframe) y auto-deploy en cada push. Intenté activarlo 100% por API con un workflow (`.github/workflows/pages.yml` + `enablement:true`), pero el token de Actions no puede CREAR el sitio Pages la 1ª vez ("Resource not accessible by integration" — requiere admin humano). Así que lo pasé a **"Deploy from a branch"** (Daniel dio el único clic en Settings→Pages) + `.nojekyll` para servir tal cual, y borré el workflow que fallaba. **VERIFICADO en vivo**: `https://danielespinolagonzalez-ux.github.io/Mistveil/` responde 200 en index, `movil.html`, `js/main.js`, `assets/audio/*.mp3`, `assets/fonts/Gelica-Regular.otf`. Se sirve todo por HTTPS; en el iPhone se abre `…/Mistveil/movil.html`. También queda un zip listo para itch.io como alternativa | El artifact seguirá con audio procedural (CSP + iframe); el sitio "real" para música/mando es el deploy de Pages. Auto-deploy: cada push a la rama de trabajo reconstruye Pages. Si el trabajo cambia de rama, hay que reapuntar la fuente de Pages | Daniel prueba en el iPhone la música y el mando en la URL de Pages; feedback de volúmenes/mapa de botones |
| 2026-07-19 | FIX AUDIO — dos quejas de Daniel en iPhone. (1) **"Las músicas se solapan continuamente"**: `playTrack` solo paraba la pista anterior DENTRO de `play().then(...)`, y en iOS ese callback puede no resolver → las pistas se apilaban sonando a la vez. Ahora `playTrack` **para en el acto (síncrono)** todas las demás pistas cacheadas (`for k in _trackEls`: clearInterval + pause) ANTES de arrancar la nueva; y el director de `main.js` solo dispara `playTrack` cuando **cambia** la pista (`musicaActual`), no cada frame. Verificado headless: máx **1 pista simultánea** en 5 cambios de escena. (2) **"Faltan SFX al disparar, no funcionan y es raro" (música sí, SFX no)**: es el **interruptor físico de silencio** de iOS — el WebAudio (beeps: disparo, golpes) va por el canal "timbre" que ese switch enmudece, mientras los `<audio>` HTML (la música) van por el canal "media" y sí suenan. Arreglo estándar tipo howler/unmute: `AudioManager._desilenciarIOS()` reproduce un **`<audio>` silencioso en bucle** (WAV de 0,5 s generado en memoria con un Blob, sin fichero) que pone la sesión en categoría "playback" y **arrastra al WebAudio al canal media** → los SFX suenan aunque el móvil esté en silencio. Se dispara solo en táctil y dentro de un gesto (`unlockMusic` + `keepAudioAlive`, que además reintenta si el 1er `play()` fue bloqueado). Verificado: node --check ×2, smoke 116/116, headless música (1 sola pista) y unmute (tag en bucle *playing*, blob ok, 0 errores) | El disparo YA emitía `sfx('tear')` (entities.js:305) y `beep`/`noise` ya reanudaban el contexto; el problema no era el cableado sino el canal de audio de iOS. Sigue pendiente por parte de Daniel: **clave de ElevenLabs inválida (401)** — bloquea generar los SFX "de verdad"; hasta entonces siguen los beeps procedurales (ya audibles con este fix) | Daniel reprueba en el iPhone: ¿música sin solaparse? ¿suenan los disparos con el móvil en silencio? Y arreglar la clave de ElevenLabs para generar SFX reales |

| 2026-07-19 | SFX REALES (19) con Magnific (Daniel: "utiliza magnific… genera pistas de 10s y córtalas"). Flujo: `audio_music_generate` (motor **elevenlabs-music-generation-v2**, 10s instrumental, prompts de sound-design "one-shot, no music, no beat") → descarga → **recorte automático** (`scratchpad/trim_sfx.py`: decodifica con ffmpeg, busca el **pico de energía**, extrae ventana desde el ataque con longitud **adaptativa** que siempre contiene pico+cola, fundido in 4ms + cola cuadrática, normaliza a −1 dBFS) → **WAV mono 44.1k** en `assets/audio/sfx_*.wav` (WAV evita el silencio de *priming* del MP3 → ataque limpio; ~640 KB los 19). Lote 1 (combate/feedback): tear, hit, enemy_die, hurt, dash, parry, cad_perfect, cad_good, gold, heart. Lote 2: cast_nova/onda/cono, explode, wax_break, door_open, cad_fail, cad_counter, clear. **AudioManager**: `loadSfx()` decodifica cada WAV a AudioBuffer una vez (forma callback de `decodeAudioData`, iOS viejo incluido); `sfx()` antepone `_playSample()` (dispara el buffer por WebAudio: baja latencia, se solapan, y suena por el canal media en iOS gracias al tag silencioso); si falta el buffer, cae al beep. Ganancia por SFX equilibrada (`_sfxGain`; tear más bajo por ir en ráfaga). Verificado: node --check, headless 19/19 buffers decodifican con su duración, `tear` usa sample, smoke 116/0. **3 clips salieron mudos** a la 1ª (tear/dash/wax_break: el modelo rinde mal con prompts "soft/airy/brittle") → detectados con `volumedetect` y regenerados con prompts "punchy/loud/strong transient"; el recortador descarta clips <−32 dBFS | El modelo es de MÚSICA, no de SFX: los efectos son aproximaciones "musicales" a auditar en el móvil; regenerable uno a uno si alguno no cuadra. Los SFX van servidos aparte (como la música): en el **bundle/artifact** (sin `assets/audio`) caen al beep; se oyen de verdad en **GitHub Pages**. `ui_tap` y otros SFX raros siguen de beep a propósito (latencia/frecuencia). ElevenLabs `text_to_sound_effects` habría sido más idóneo pero su clave sigue 401 | Daniel audiciona los 19 en la URL de Pages y me dice cuáles regenerar; opcional 3er lote (equip/tome/armor, door_seal, finisher, no_sp) |
| 2026-07-19 | MÚSICA EN BUCLE SIN COSTURA (Daniel: "loop infinito, lo más cuadrado posible; revisa dónde encaja, únelo con un fade"). El motor pasa de HTMLAudio `loop=true` (que al reiniciar dejaba oír el **silencio de los extremos** del clip + el salto del MP3) a **WebAudio `source.loop=true`** (bucle exacto de muestra). Al cargar cada pista en loop, `_makeSeamlessLoop` hornea en memoria un **crossfade cola→cabeza**: recorta los silencios de los extremos del buffer decodificado (así el *priming* del MP3 no rompe la unión), **busca por correlación** el largo de crossfade donde cabeza y cola mejor casan, y funde cola sobre cabeza a **potencia constante** (sin/cos) → el final enlaza con el principio sin salto ni hueco. `playTrack` usa AudioBufferSource+GainNode con crossfade entre pistas por rampa; caché LRU de 3 buffers; el máster respeta `vol_musica` (refrescado en cada cambio). Suena por el canal media en iOS gracias al tag silencioso. **Fallback**: si WebAudio/decode falla (bundle sin `assets/audio`) cae a `<audio>` loop, y el dron procedural solo se ducka cuando suena la pista real → el bundle no se queda mudo. One-shots (derrota/victoria) y stingers sin cambios. Verificado headless: las **9 pistas de loop** quedan con energía continua en la unión (final≈principio, ratio x1.0–x1.4; recortes de 1,5–8,7 s según dónde casa), one-shot sin recorte, el dron sobrevive a una pista inexistente; smoke 116/0 | El crossfade es de segundos (blend largo, natural en orquestal/ambiental); si alguna pista suena "doblada" en la zona de fundido, bajar el máximo de candidatos o regenerar la pista con final que case mejor. El volumen en caliente aún no se aplica a una pista **ya sonando** (solo al cambiar); deuda menor. `mus_santuario` es la más floja (ambiente), unión igualmente casada | Daniel escucha los bucles en Pages/iPhone: ¿enlazan sin nota de corte? Si alguno chirría en el fundido, lo afino/regenero |
| 2026-07-19 | COMBATE — telegrafía clara + jefes con variedad + dificultad inteligente (petición de Daniel: "las mecánicas de los jefes son iguales para todos; no se ve venir el ataque; la Ignición se rellena muy fácil; el juego es fácil, súbelo de forma inteligente sin subir daño"). 3 agentes Explore mapearon jefes / telegrafía de enemigos / economía SP+dificultad ANTES de tocar nada. **Fase 1 — telegrafía universal**: los enemigos a distancia (turret/orbit/spiral/cuco/sanador) disparaban "en seco"; ahora AVISAN con un anillo que se cierra hacia ellos + ◆ sobre la cabeza, coloreado por **familia** (cera cálida / latón dorado / polvo violeta), y **bloquean el apuntado** al avisar (esquivas moviéndote). `entities._avisoDisparo` + estado cast/castAim/muzzle; `balance.enemigo_rango.telegraph_s`. El anillo/arco de melé y el ◆ se tiñen por familia (parry sigue en cian); nuevos handlers enemy_cast (◆) y enemy_strike (antes SIN feedback → fogonazo+shake). **Fase 1b — jefes**: FIX del bug `telegraph_s` (no llegaba a la zona → aviso descalibrado en casi todas), destello blanco pre-impacto en el último ~20%, el barrido se pinta como CUÑA del ancho real (no una raya engañosa); la colisión círculo/donut/safe ya casaba con la elipse (proyección 2.5D) — no se tocó. **Fase 2 — mecánica FIRMA por jefe**: cada uno gana una mecánica que ningún otro usa → Campanero **ondas** (anillos que se expanden), Carillón **cadena** (rayo encadenado por nodos), Archivera **proyectiles** (orbes de tinta reales), Relojero **roba** (te quita el compás unos s; reconecta el legacy roba_compas muerto). Campanero y Carillón dejan de ser gemelos. **Fase 3 — Ignición cara + dificultad inteligente (sin tocar daño bruto)**: `ignicion.sp_max` 100→140, Febril `sp_mult` 1.5→1.25 (era el atajo), NUEVO decay de SP fuera de combate (`ignicion.sp_decay_por_s` 4, `sp_decay_solo_fuera_combate`) para que no se banque Ignición entre salas; más densidad (`dificultad_sala_base` 4→5, `dificultad_por_piso` 2.25→2.5) y menos sostén (`drop_corazon_pct` 18→12). Verificado: node --check, smoke **123/0** (+7 asserts: telegrafía de rango + firma exclusiva por jefe), driver headless de los 4 directores 40s×2 fases (cada firma aparece, 0 errores) y capturas en-motor (anillos+◆ por familia; donut del jefe con zona segura cian; carga de ondas). 3 commits separados; hook __god de captura retirado antes de cada commit | Todo lo de dificultad es data-driven y afinable con F5 (Daniel dial fino jugando). La telegrafía de rango hace a los enemigos a distancia MÁS esquivables (a propósito, claridad), la dificultad la aportan densidad+menos sostén+Ignición cara, no el daño. Pendiente que Daniel valide en iPhone: ¿se ven venir los ataques? ¿los jefes se sienten distintos? ¿la curva de dificultad cuadra o hay que afinar los números? | Feedback de Daniel jugando; afinar valores de dificultad/telegraph_s; opcional: más firmas por jefe, o telegrafía propia de más enemigos de melé "planos" |
| 2026-07-19 | AUDITORÍA del juego vs. el género (petición de Daniel) con un panel de 9 analistas en paralelo (uno por dimensión: combate/game feel, sistema de ritmo, progresión intra-run, meta/rejugabilidad, contenido, narrativa, UX/accesibilidad, móvil/plataforma, audio/arte) + síntesis, comparando con Hades/Dead Cells/Isaac/Gungeon/NecroDancer/Hi-Fi Rush/Metal Hellsinger/Vampire Survivors/Slay the Spire/roguelites móviles. **Informe completo en `docs/auditoria_2026-07.md`** (134 KB) y **hoja de ruta A–E añadida a ROADMAP.md**. Veredicto: corazón excelente, cuerpo a medio construir (combate 8/10, identidad rítmica *cumplida* 4/10, build/endgame 4/10, narrativa 3/10, producto móvil 5/10, UX/opciones 3/10). 3 huecos grandes: (1) la promesa "reloj y RITMO" sin cumplir (la música no es el reloj; ventanas fijas 700ms, SFX de tono fijo, bucle de paso variable) → hoy es QTE rítmico estilo Legend of Dragoon, no NecroDancer; (2) profundidad/retención cortas (20 items, sinergias cosméticas, pedestal sin elección, endgame de un interruptor); (3) producto móvil a nivel prototipo (no se guarda la run, sin opciones ni volumen, sin calibración de latencia, guardado se evapora en iOS a 7 días). Hilo transversal esperanzador: casi toda carencia es UI/contenido/reglas sobre motor YA construido (window_scale, gains de volumen, beat del Metrónomo, semilla determinista, rollItem, pestaña Crónica). Decisión de Daniel: **guardar roadmap → terminar menús móviles → arrancar Fase A (ritmo)** | La auditoría reordena por impacto pero NO borra las R-tareas; las fases A–E cruzan con R2.5/R3.x/R4.0/R5.x. Fortalezas a preservar intactas: Cadencia (única fuente de SP, techo de habilidad real), juice/legibilidad de nivel referente, ingeniería táctil, 16 IAs, 4 jefes con firma | Ejecutar: menús móviles (kit UI ya diseñado) y Fase A (acople música-beat-input) |
| 2026-07-19 | MENÚS MÓVILES + NAVEGACIÓN POR MANDO (Daniel: "los menús son muy pequeños para móvil; reinvéntalos como un juego de móvil; y se usan con mando"). 2 agentes Explore mapearon todos los menús y el estado del mando; panel de diseño (3 enfoques + juez) eligió el pragmático + hit-inflación + glyph bar. **input.js**: `justDir()` (d-pad 12-15 + flanco del stick izq, anti-rebote + auto-repeat 380/140ms) enchufado en `justPressed('move_*')` → el menú de equipo (Equipo+Esfera) y la Batalla, que YA tenían foco, quedan navegables con mando SIN tocarlos; acciones `ui_confirm`(A)/`ui_cancel`(B)/`ui_tab_prev`(LB)/`ui_tab_next`(RB). **Nuevo `js/ui_kit.js`** (aislado, solo dibujo+hit-test+foco): botón/panel/texto con mínimo táctil ≥44px, `hit()` que infla el rect a 44, `glyphBar` que adapta glifos a la fuente activa (mando (A)/(B)·toque 'Toca'·teclado), helpers `mover1D/moverGrid/confirmo/cancelo` que leen `justPressed('move_*'/'ui_*')`. **Reconstruidos** con foco navegable por toque/mando/teclado + tipografía grande: **Pausa** (lista de botones), **Mejora** (foco de carta + halo), **Santuario** (rejilla 4-col + Descender botón grande 200×44, nombre con fitFont, fuentes 7→8/8→9), **Esfera del Reloj** (nodos 6-7→8-9px, radio táctil 13→22px = por fin tocable con el pulgar, pestañas 16→22). Se mantiene LANDSCAPE (portrait sigue muerto). Verificado: node --check, smoke 123/0, capturas móvil de Pausa/Mejora/Santuario/Esfera (botones grandes, foco pulsante, textos legibles) + nav de mando verificada en input.js (d-pad/stick mueven foco, A/B, LB/RB). Hooks de captura retirados. **Diseño completo guardado** en el panel para iterar | Pendiente de pulido (no crítico): pase de fuentes de la CRÓNICA (font(7), necesita relayout) y filas del EQUIPO; el ✕ cerrar del menú sigue pequeño pero B/Start/Esc/Tab lo cierran. El gameplay táctil (Una Mano, sticks) intacto | Daniel prueba menús en iPhone + con mando; luego pulido de Crónica/Equipo si hace falta |
| 2026-07-19 | FASE A (acople ritmo↔feel) — primer bloque, el clúster de mayor ROI de la auditoría (#8). **A3 juice del disparo**: la lágrima al impactar da chispazo DIRECCIONAL teñido por elemento + núcleo blanco + micro-shake; al rematar, micro-hit-stop 0.045s + shake (antes el disparo, la acción más repetida, era la más sosa). **A2 SFX en escala**: `AudioManager.sfx/_playSample` aceptan `pitch` (playbackRate del buffer); la lágrima varía el tono ±semitonos (anti-ametralladora) y los golpes de Cadencia ASCIENDEN de tono con el índice del combo (arpegio de tonos enteros que culmina en el finisher; el fallo no sube) → el combo "suena a melodía". Sin assets (audio sintetizado). Verificado: node --check ×4, smoke 123/0, headless sin errores (testCad ejercita el arpegio) | **Pendiente de Fase A** (las piezas de identidad que conviene probar en el iPhone): A1 reloj de beat global en AudioManager con BPM por pista + anclar las ventanas de Cadencia al pulso (la carencia #1, esfuerzo alto, riesgo si se hace a ciegas); A4 latido visual del HUD al pulso; A5 música reactiva a la racha. A1 es el centro de "reloj y ritmo" pero toca el timing de Cadencia → hacerlo con Daniel probando la sincronía en móvil | Daniel prueba el juice/arpegio; A1 (beat clock) como siguiente pieza focalizada, verificando el sync en el iPhone |
| 2026-07-19 | FASE B — PANTALLA DE OPCIONES (cierra 2 carencias CRÍTICAS de la auditoría: #3 "sin opciones ni volumen ajustable" y #4 "sin calibración de latencia"; desbloqueada por el kit de UI). Nuevo modo `'opciones'` abierto desde Pausa ("Opciones"), navegable por toque/mando/teclado con foco: **Volumen música** / **Volumen efectos** (sliders), **Asistencia de ritmo** (ensancha las ventanas de Cadencia, para accesibilidad), **Calibración de latencia** (±120 ms), toggle **Control táctil**, y **Volver**. Kit: nuevo `UI.slider` (barra tocable/arrastrable, ajustable con izq/dcha del foco). Cableado real y verificado: el volumen de música se aplica EN CALIENTE (`AudioManager.setMusicVol`), `escala_ventanas` multiplica las ventanas en `cadencia.js`, `latencia_ms` desplaza el juicio en el punto de uso (`computeQuality(this.t - lat, …)`, sin tocar la función pura testeada). `opciones.latencia_ms` añadido a los defaults (merge defensivo en la carga). Verificado: node --check ×4, smoke 123/0, captura móvil (sliders grandes, foco pulsante, los 6 ítems caben) + edición del estado sin errores | El toggle de control táctil se movió de Pausa a Opciones (Pausa queda: Continuar + Opciones). Falta aún de Fase B: B1 guardar/reanudar run (serializar RunState), B4 export/import de guardado, B5 paso fijo, B6 PWA/safe-area. La calibración es por slider manual; una cámara de calibración por beats (reusando el Metrónomo) es mejora futura | Daniel prueba Opciones en el iPhone (bajar volumen, asistencia, latencia); siguiente Fase B: guardar/reanudar run |
| 2026-07-19 | FASE A — **A1 RELOJ DE BEAT** (la carencia #1 de la auditoría: "la música no es el reloj"), hecho de forma AUTÓNOMA pero de BAJO RIESGO: el reloj late VISUALMENTE con la música sin tocar el juicio de input de Cadencia (que sigue en `computeQuality`, función pura testeada). Scout de 2 agentes mapeó la infra de ritmo y la de guardado ANTES de tocar nada. **BPM medido offline**: script Python (ffmpeg→PCM, onset por flujo espectral + autocorrelación ponderada hacia ~110 BPM) sobre las 11 pistas → `data/musica.json` con `{bpm, offset_s}` por pista (offset relativo al inicio RECORTADO del bucle, módulo un beat, para que el pulso caiga EN los golpes; replica el recorte de silencio de `_makeSeamlessLoop`). **AudioManager.beatClock()**: ancla `ctx.currentTime` (sample-accurate) al arranque de la pista + su BPM → `{active,bpm,phase 0..1,beat,pulse}`; inactivo si no hay pista WebAudio o no hay BPM medido (degradación limpia; el fallback HTMLAudio no lo activa). Guarda `_musStartTime/_musBpm/_musBeatOffset` en `playTrack`, los limpia en `_stopMusicNow`. **Latido en 3 sitios** (todo aditivo): marco de la barra SP destella al beat, un **pip** junto al banner de compás ♪ se enciende en cada golpe, y el **anillo de Cadencia** gana un halo pulsante al beat (inyectado con `cad.beat`, SOLO visual — el timing del golpe NO cambia). **Tic-tac ambiental** (60 BPM por setTimeout) ahora se CALLA mientras suena una pista real (dos relojes ya no pelean; la pista es el reloj). Verificado: node --check ×4, smoke **129/0** (+6 asserts: musica.json con BPM, `musicaBpm` null si no medido, `beatClock` inactivo sin pista + activo/fase/índice/pulse con pista simulada), captura en-motor con reloj forzado (pip y glow renderizan, 0 errores) | Los BPM detectados tienen confianza mixta (2–7); los lentos (archivo/jefe/truenos ~70, invertida ~66) son legítimamente lentos (exploración) y los de combate más rápidos (péndulos ~117, redoble ~129) — la ambigüedad de octava es inocua para un pulso (medio tempo cae igual en beats). El pulso es HONESTO al beat detectado (no forzado). NO se ancló el input al beat (riesgo de romper el feel testeado); eso queda para validar con Daniel jugando. Todo es data-driven: recalibrar un BPM = editar `musica.json` + F5 | Daniel valida en iPhone si el latido se siente EN la música; si sí, considerar (con su ok) anclar el arranque de cada golpe de Cadencia al beat (A4 ambicioso). Sigue B1: guardar/reanudar run |
| 2026-07-19 | FASE B — **B1 GUARDAR/REANUDAR RUN** (carencia CRÍTICA de la auditoría: "no se guarda la run; iOS la evapora a los 7 días"). Ahora la partida a medias sobrevive a cerrar la app / recarga. **SaveManager** gana `saveRun/loadRun/hasRun/clearRun` en su propia KEY `mistveil_run` (schema 1, patrón tolerante `?? defecto`, aparte del meta). **Snapshot** (`buildSnapshot` en main.js): RunState (piso, semilla, oro, sp, items, tomos, hechizo, compas —resolviendo `compasRobado`—, mejoras, activoSalas, selloSpAcum, arteGratis, floorStats, contrato) + Player (hp y **maxHp**, que cubre los deltas de corazón de altares/pan que NO están en items) + estado de piso (sala actual, y por sala: cleared/visited/looted, pedestal/pedestal2/stock/altares consumidos, pos del jugador). NO serializa mods (derivados de items) ni geometría (regenerada). **Reanudar** (`restoreSnapshot`): carga GameState → `buildTower(piso,semilla)` → restaura flags de sala → **reconstruye el Player re-aplicando items** (mismo patrón que la Fundición) y sobrescribe hp/maxHp → coloca al jugador en la sala/posición guardada → `mode='play'`. **Autosave** en puntos seguros: `room_cleared`, al **pausar**, al **descender** de piso y al entrar a una sala NO sellada (captura reliquias de pedestal/tienda). Se **borra** al morir, en victoria y al empezar descenso nuevo (pueblo/Santuario). **Título**: si hay partida + tutorial hecho, dos botones grandes navegables **REANUDAR — Piso N** / **PUEBLO (nueva partida)**. **Hallazgo clave**: `buildTower` es determinista en TOPOLOGÍA (tipos de sala, alcobas, jefe) pero NO en interiores ni en posiciones de roamers (las plantillas de sala usan `Math.random`, que desincroniza el RNG sembrado) → por eso el restore descarta los roamers de galerías ya visitadas en vez de reproducirlos exactos, y el item de un pedestal solo-ojeado puede variar (aceptable). Verificado: node --check, smoke **138/0** (+9 asserts: reloj de beat, roundtrip de SaveManager, determinismo de topología/jefe) y **prueba de integración real** (jugar piso 3 con build+oro77+sp33+hp3 → pausar → autosave → RECARGAR la página → título muestra REANUDAR → Enter → vuelve a play en piso 3 con TODO idéntico, 0 errores) + capturas del menú de título navegable. Hook de depuración `__dbgB1` retirado antes del commit | Bug pillado y corregido: el arranque llama `newRun()` como andamiaje detrás del título, y `newRun` borraba la run guardada → se movió el `clearRun` a los sitios donde el JUGADOR decide empezar de cero (descenso pueblo/Santuario). Limitación consciente: si guardas a mitad de un combate, al reanudar la sala en curso no se reproduce el mismo encuentro (enemigos por `Math.random`); por eso el autosave prioriza salas ya limpias. Falta aún de Fase B: B4 export/import de guardado, B5 paso fijo, B6 PWA/safe-area | Daniel prueba en iPhone: jugar, cerrar la app a mitad, reabrir y REANUDAR. Siguiente: B4 (export/import) o volver a contenido (R3 pisos 4–9) |
| 2026-07-19 | **RECONCILIACIÓN + A5** (Daniel: "sigue con las siguientes mejoras del roadmap"). Workflow de 4 agentes (ultracode) mapeó en paralelo el estado real del ROADMAP y A5/B5/B6. **Hallazgo**: A2/A3/A4/B2/B3 YA estaban hechos (evidencia en código) pero sin marcar → ticados. También la fuente YA es Gelica LOCAL (no VT323/Google Fonts): corregida la trampa obsoleta de CLAUDE.md, R5.1 cumplido, y cerrada la última fuga de Google Fonts (playground de dev). **A5 — MÚSICA REACTIVA A LA RACHA** (cierra Fase A): capa procedural de campanas armónicas (`AudioManager.grooveBell`) que suena EN el beat (programada por `beatTime()` en `ctx.currentTime`, sample-accurate) cuando el `grooveFrac` supera un umbral; la intensidad la marca la racha (perfects), con octava extra en buff, y se APAGA SOLA al decaer el groove. Cuelga de un sub-bus propio (`_reactNode`) bajo el bus de música → respeta el volumen y NO pisa la pista ni su fade. Conducida por POLLING del bucle (detector de borde de beat en main), NO por EventBus.on (el bus no tiene off, fugaría). `reactiveGain()` es helper PURO testeado. Todo data-driven (`balance.musica_reactiva`, F5). Verificado: node --check, smoke **144/0** (+6 asserts: beatTime, reactiveGain puro monótono, grooveBell tolerante sin ctx, presencia de config) y prueba en-motor con espía (groove 100 → 4 campanas en 4 s = cadencia correcta cada 2 beats, intensidad decayendo 1.0→0.82 con la racha, octava de buff activa, 0 errores) | Fase A COMPLETA (A1–A5): el ritmo se acopla a la música (reloj de beat + latido HUD + arpegio + juice + capa reactiva). B5 (paso fijo) DEFERIDO: es carencia #11 (no crítica) y el plan seguro exige trocear el bloque de física del god-file `update()` — cirugía que merece su propia sesión con Daniel validando el feel; el mapa está guardado. **OJO licencia**: Gelica es fuente comercial (Zetafonts) — verificar derecho de redistribución antes de publicar repo/bundle | B6 (PWA + safe-area) a continuación; luego Fase C (build/endgame) o R3 (contenido pisos 4–9) |
| 2026-07-19 | **B6 — PWA + SAFE-AREA** (mobile-first; cierra R5.1/R5.2). **PWA**: nuevo `manifest.json` (rutas RELATIVAS por la subruta de GitHub Pages, standalone+landscape, iconos de Pip 192/512/maskable) + `sw.js` (service worker: navegación network-first, estáticos cache-first con refresco; el audio de 11 MB se cachea al usarse, no en install) + `<link manifest>`/`apple-touch-icon`/`theme-color`/`viewport-fit=cover` en index/movil + registro de SW guardado (solo http(s), no rompe el bundle). **Iconos** generados del retrato de Pip (Pillow) sobre el fondo temático. **Probado de verdad**: con el SW controlando y la RED CORTADA, el juego arranca offline y hasta la portada pintada carga de caché. **Safe-area/notch**: los botones táctiles pegados a los bordes se apartan de la muesca/indicador de inicio del iPhone — sonda DOM de `env(safe-area-inset-*)` convertida a coords virtuales descontando el letterbox del canvas centrado; los botones comparten objeto por índice, así mutar `x/y` sincroniza dibujo y hit-test solos. Verificado: sin muesca los botones NO se mueven (regresión de escritorio), con muesca 44/34 px los de la derecha van a la izq 44 y los de abajo suben 34, y vuelven a base al quitarla. **Fuente ya local** (Gelica): corregida la trampa obsoleta; cerrada la última fuga de Google Fonts (playground). **BONUS — arreglado el bundle single-file roto**: `tools/build_artifact.mjs` no sabía tratar `import * as UIK` (de los menús) ni imports con comentario al final → el bundle petaba ("Unexpected token"); ahora los soporta y embebe `data/musica.json`. Verificado: node --check, smoke **148/0** (+4 asserts PWA), prueba offline y prueba de safe-area con muesca simulada, y el bundle vuelve a arrancar limpio. Hook `__dbgSafe` retirado | **OJO licencia**: Gelica es fuente COMERCIAL (Zetafonts) — redistribuirla en repo público/itch.io puede infringir; verificar con Daniel de dónde salió el .otf y su licencia web. Si no cubre redistribución, volver a una serif OFL. B5 (paso fijo) sigue deferido (riesgo). Falta de Fase B: B4 (export/import de guardado). | Daniel: probar "Añadir a inicio" en el iPhone y jugar offline; decidir licencia de Gelica. Siguiente: Fase C (profundidad de build: C1 reliquias, C2 pedestal elige-1-de-3, C3 sinergias, C4 Heat) o B4 |
| 2026-07-19 | **MAPA LABERÍNTICO** (petición de Daniel: "salas diferentes... distribución más laberíntica y aleatoria... sube la dificultad"). Increment 1 de 4: reescrito `buildTower` de una columna vertical con alcobas a un **GRAFO 2D RAMIFICADO que desciende**: un camino principal que baja serpenteando (paseo aleatorio con peso al sur + vaivén lateral) del que brotan RAMAS/callejones (que hospedan las salas especiales) y algún BUCLE (ciclos). Inicio arriba, jefe al fondo → conserva "La Torre desciende", pero cada piso es ahora un laberinto de 15-19 salas (antes 9-13) con múltiples caminos. **La infraestructura ya lo soportaba** (rejilla gx/gy, puertas n/s/e/w, at()/neighbor(), minimapa 2D, cámara por bbox, clampPlayer por 4 lados) — solo `buildTower` era vertical. Determinista por semilla en TOPOLOGÍA (solo `rnd` sembrado; los interiores de Room siguen con Math.random). Tesoro/tienda GARANTIZADOS en puntas de rama; maldita/cámara especial opcionales; huecos para `evento`/`desafio` (gateados a prob 0 hasta implementar su comportamiento en el siguiente increment). Knobs data-driven en `balance.run.laberinto_*` (F5). **Asserts de humo reescritos**: la vieja comprobación de "columna vertical conexa" se sustituyó por **BFS de conectividad de grafo** (anti-softlock real: inicio alcanza TODAS las salas + jefe) y simetría de puertas. Verificado: node --check, smoke **148/0** (60 torres: grafo conexo, puertas simétricas, tesoro/tienda/jefe presentes), volcado estructural de 3 pisos (formas claramente laberínticas) y captura en-motor (ramas E/S visibles, minimapa 2D, navegación OK, 0 errores) | La dificultad SUBE de rebote (más salas = más combate/atrición antes del jefe). Faltan 3 increments: (2) dificultad inteligente con modificador ÉLITE, (3) eventos-altar data-driven (trato/sacrificio/fuente...), (4) salas-desafío cronometradas. Las salas `evento`/`desafio` ya se generan pero están a prob 0 hasta tener su lógica. Nota: el workflow de mapeo tocó el límite de sesión y 3 de 4 agentes no terminaron; tiré de lectura propia (tenía el mapa del generador de B1) | Increment 2: dificultad (élites) — la petición más enfática de Daniel |
| 2026-07-19 | **DIFICULTAD INTELIGENTE — modificador ÉLITE** (Increment 2/4; "sobretodo regula la dificultad, ahora es muy fácil"). Cualquier enemigo normal puede PROMOCIONAR a ÉLITE (minijefe ligero) en `_rollSpawns`, con probabilidad que sube por piso (`elite.prob_base` 0.10 + 0.035/piso, tope 0.40). Un élite = +vida (×2.4), mayor (×1.25), **aura de color que DELATA su rasgo** (acorazado azul acero / veloz cian / iracundo rojo) con doble aro para destacar entre la horda, y un RASGO data-driven: **acorazado** encaja −45% de daño (aguanta más ciclos de Cadencia), **veloz** +55% velocidad (te caza), **iracundo** +30% velocidad y dispara ~40% más rápido. Al caer suelta **botín garantizado** (oro + espíritu, y 24% de corazón) — recompensa el riesgo sin regalar sostén. Todo en `balance.elite` (F5). **Tuning base** además: presupuesto de sala `dificultad_sala_base` 5→6 y `dificultad_por_piso` 2.5→3.0 (con el laberinto = más salas, la atrición sube), y menos red de seguridad `drop_corazon_pct` 12→9. NO se tocó el daño base. Verificado: node --check ×3, smoke **153/0** (+5 asserts: balance.elite, élite con más vida/tamaño, acorazado encaja menos daño, veloz más rápido, normal no-élite) y captura en-motor de los 3 élites (auras distintas legibles, minijefes claros). Hook temporal `__dbgElite` retirado | La curva sube por: laberinto (más combate) + élites (minijefes) + presupuesto + menos corazones. TODO tunable por F5 — Daniel dial fino jugando. Faltan increments 3 (eventos-altar) y 4 (salas-desafío). El rasgo "explosivo" se aparcó (necesita plumbing de daño AoE al jugador; los 3 actuales no tocan rutas críticas) | Increment 3: eventos-altar data-driven (trato/sacrificio/fuente/confesión) |
| 2026-07-19 | **SALAS-EVENTO data-driven** (Increment 3/4; "salas diferentes... eventos especiales"). Nuevo tipo de sala `evento` con un ALTAR interactivo que dispara un evento de `data/eventos.json`, interpretado por el nuevo `js/eventos.js` (espejo de items.js: `aplicarEvento(def, api)` cobra coste, aplica `efectos[]` fijos y una salida `azar[]` ponderada; `pickAzar` es PURO y testeable). main solo inyecta la `api` con acceso al mundo (oro/sp/corazones/dar_item/emboscada/curar). **4 eventos de arranque**: **El Trato del Prestamista** (cofre trampa: reliquia de tesoro + precio oculto por azar — corazón / −oro / emboscada), **La Fuente de Cuerda** (bebe: cura total / +1 / −corazón), **El Relicario Sellado** (paga 70 SP → reliquia de jefe), **La Moneda del Tiempo** (−1 corazón → +oro y +SP). Añadir uno más = 1 entrada JSON, cero código. El dibujo de altares se hizo TOLERANTE (glifo/color/label con fallback → cada evento trae su glifo/color); icono de minimapa (`evento` dorado) + flash de entrada + `evento_prob` 0.55 (F5). La persistencia B1 ya cubre el estado (usa `al.used`, serializado). Verificado: node --check ×4, smoke **159/0** (+6 asserts: eventos.json carga, cada evento con id/glifo/color, pickAzar por peso, evento rechaza sin pago, cobra+aplica, azar aplica una salida) y captura en-motor del altar (glifo "≈" cian de la Fuente, sobre pedestal, icono dorado en el minimapa). Hook temporal `__dbgGoto` retirado | Migrar fundición/metrónomo/apuesta a este sistema unificaría el switch de activarAltar (futuro). Falta increment 4 (salas-desafío cronometradas/oleadas, reusando beatClock + des-sellado por condición no-combate). El `desafio_prob` sigue a 0 hasta implementarlo | Increment 4: salas-desafío |
| 2026-07-19 | **SALAS-DESAFÍO** (Increment 4/4, ÚLTIMO del overhaul de la Torre). Nuevo tipo `desafio`: cámara de combate REFORZADA (presupuesto ×1.8 + un MINIJEFE élite garantizado) con un **reloj de BONUS**. CLAVE de diseño seguro: el tiempo NO bloquea la salida — la sala se limpia matando a todos como cualquier cámara (des-sellado normal, CERO riesgo de softlock); el reloj solo decide si te llevas el bonus. Al limpiar: siempre una reliquia; si fue A TIEMPO (`desafio.tiempo_base_s` + escala por piso), BONUS de oro + opción de corazón. HUD: barra de cuenta atrás cian que se pone roja y parpadea al agotarse ("BONUS PERDIDO — ¡acaba con ellos!"). Banner de entrada, icono de minimapa. Todo data-driven (`balance.desafio`, F5); `desafio_prob` 0.4. La persistencia B1 lo cubre (el estado del reto es transitorio de combate; B1 solo guarda salas ya limpias). Verificado: node --check ×2, smoke **161/0** (+2 asserts: la sala desafío tiene reto con límite y es de combate con spawns; el laberinto sigue conexo con desafíos), y captura en-motor (barra "BONUS EN 23.6s", banner, oleada con minijefe de aura, telegrafía). Hook temporal `__dbgGoto` retirado | **OVERHAUL DE LA TORRE COMPLETO (4/4)**: (1) mapa laberíntico, (2) dificultad élite+tuning, (3) eventos-altar, (4) salas-desafío. La Torre pasó de columna con alcobas a laberinto 2D de 15-19 salas con minijefes, eventos de decisión/azar y retos cronometrados; la dificultad sube por densidad+élites+menos sostén, no por daño bruto. TODO tunable por F5. Reto data-driven de "oleadas" quedó como ampliación futura (el intérprete de modos existe conceptualmente; hoy solo 'cronometrado') | Daniel prueba el nuevo mapa/dificultad en el iPhone y afina números por F5; luego Fase C (build/endgame) o E (narrativa) |
| 2026-07-19 | **AFINADO DE DIFICULTAD por medición** (Daniel: "sigue con F5"). Medí la curva REAL headless (30 torres × 12 pisos, contando salas de combate, enemigos, coste y élites por piso llamando a `_rollSpawns`). Diagnóstico: la curva de enemigos era buena (13→~27, se estabiliza al comprar enemigos más caros) pero los **élites se disparaban** (1.4 en piso 1, hasta 10/piso al final por el tope de prob 0.40 = 40% de la horda eran minijefes → dejaban de sentirse "mini-JEFE" y era un muro de esponjas). **Reajuste**: `elite.prob_base` 0.10→0.06, `prob_por_piso` 0.035→0.014, `prob_max` 0.40→0.20, y NUEVO `max_por_sala` 2 (tope de élites por sala, cableado en `_rollSpawns`). Curva resultante medida: élites **1.1 → 2.7 → 4.6** de piso 1 a 12 (antes 1.4→10.3) — se mantienen ESPECIALES, con la densidad de enemigos llevando el peso de la dificultad. **Verificado F5**: `DataDB.reload()` recarga los nuevos `eventos.json`/`musica.json`/bloques de balance en caliente con 0 errores → Daniel puede dial-fino en vivo. Nuevo **`docs/balance_torre.md`**: curva medida + TODOS los diales de F5 explicados (qué sube/baja la dificultad) + recomendaciones. node --check, smoke **161/0** | La dificultad ahora escala por densidad+élites-especiales+menos sostén, no por bultos ni daño bruto. Todo sigue tunable por F5; el doc guía el afinado fino de Daniel. El salto piso 1→2 (+58% enemigos por `dificultad_por_piso` 3.0) es notable pero intencional (piso 1 = intro suave); bajable a ~2.2 si molesta | Daniel prueba y ajusta por F5 con el doc; luego Fase C (build/endgame) o E (narrativa) |
| 2026-07-19 | **APARIENCIA POR OBJETO + AMPLIACIÓN DE RELIQUIAS** (Daniel: "ampliación de objetos... ¿cada objeto con apariencia diferente?"). Respuesta: SÍ. **Hallazgo**: las 20 reliquias YA tenían icono pintado (`icono_<id>`) pero SOLO se usaba en el menú; en el SUELO salían como un rombo genérico del color del tag → todas iguales. **Arreglo (2 vías)**: (1) el pickup del suelo ahora usa el icono PINTADO (misma imagen que el menú); (2) nuevo **`js/item_icons.js`** con `drawItemIcon(forma,color)` — ~18 SILUETAS dibujadas por código (gota/doble_gota/reloj/engranaje/pendulo/campana/llama/ojo/ala/corazon/luna/peso/resorte/bateria/frasco/espejo/pluma/cuerda/sello) como fallback para reliquias SIN arte pintado, en suelo Y menú → toda reliquia se ve distinta sin pedir assets (regla 6, "el arte se dibuja"). **Ampliación**: +10 reliquias nuevas (20→30) compuestas SOLO con efectos ya interpretados por el motor (stat/tear_flag/cadencia/ignicion/on_event), cada una con su `icono_forma`+`icono_color`: Muelle tenso, Contrapeso de latón, Cristal de enfoque, Cuerda de plata, Aguja de tinta, Rescoldo perpetuo, Corazón de cuerda, Escarcha del péndulo, Reloj de arena roto, Ala de sombra. +2 sinergias nuevas. Verificado: node --check ×2, smoke **165/0** (+4 asserts: roster ≥30, toda reliquia con forma, la nueva sube el alcance, drawItemIcon pinta todas las formas sin romper) y **rejilla visual de las 30 siluetas** (todas distintas y legibles). Cero assets nuevos | Faltan de esta tanda: +eventos (data-only, trivial) y +enemigos (data-driven con fallback por código). Los 10 items nuevos no tienen arte PINTADO (usan el procedural); si Daniel quiere, se generan luego con Recraft. El motor de sinergias (C3) recibió 2 pares nuevos gratis | Sigo con eventos + enemigos nuevos |
| 2026-07-19 | **+EVENTOS y +ENEMIGOS** (cierre de la tanda de ampliación). **Eventos**: +3 a `eventos.json` (4→7, cero código): La Ruleta del Reloj (azar puro: oro/SP/emboscada/reliquia), La Forja del Gremio (paga 22 oro → reliquia de jefe), El Pozo de los Deseos (−1 corazón → reliquia + oro). **Enemigos**: +4 a `enemigos.json` (19→23) reusando comportamientos existentes: Cascabel de latón (orbit_shoot), Esquirla de polvo (charger), Sebo hirviente (chase_explode), Centinela de polvo (turret); repartidos por pools de bioma. **Fallback de dibujo por familia**: el `default` del switch de `drawEnemy` (antes un círculo plano) ahora pinta un cuerpo DISTINTO por familia — cera (blob cálido con ojos), latón (rombo dorado angular), polvo (wisp frío difuso con halo) — así un enemigo nuevo SIN sprite pintado se lee como criatura, no como bolita. Verificado: node --check, smoke **169/0** (+5 asserts: roster ≥23, los 4 con comportamiento válido+familia+instancian+en pool, eventos ≥7), prueba en-motor (los 4 spawnean y se comportan 2.5s, 0 errores; auras/cuerpos de familia legibles) y el bundle single-file vuelve a arrancar con el módulo item_icons. **TANDA COMPLETA**: apariencia por objeto + 10 reliquias + 3 eventos + 4 enemigos, todo data-driven y con dibujo por código (cero assets nuevos) | Los enemigos nuevos usan el fallback por familia (no arte pintado propio); si Daniel quiere sprites únicos, se generan con Recraft luego. Igual con los 10 items nuevos (usan icono procedural). El motor no necesitó código nuevo salvo el generador de iconos y el fallback de familia | Daniel prueba la variedad nueva; luego arte pintado de lo nuevo (Recraft) o Fase C/E |
| 2026-07-19 | **FASE C — C2 (pedestal elige-1-de) + C3 (conjuntos/set-bonus)** (Daniel: "Fase c"). **C2 — decisión de build en el tesoro**: la sala de tesoro ya NO da una reliquia automática; ahora presenta **2 pedestales** (3 con la reliquia "Páginas perdidas") con reliquias distintas y coger UNA **desvanece las demás** → mismo botín que antes pero con elección. `Room` genera `pedestal/pedestal2/pedestal3` desde un array `elegidos` (reusa `rollItem`, sin repes); `take/draw/shadow` iteran sobre los 3; al recoger uno se marcan los otros como looted. La persistencia B1 serializa `pedestal3`. **C3 — motor de conjuntos por etiqueta** (`items.conjuntos`): reunir N reliquias con el mismo `tag` desbloquea un **efecto de conjunto** (reusa el formato de `efectos[]` de item). `items.js` se factorizó: `aplicarEfectos(efectos, player, origen)` extrae el switch (antes anidado en el bucle de item) y ahora lo comparten items, sinergias y conjuntos; `applyItemEffects` recorre `DataDB.items.conjuntos` tras equipar, detecta el umbral **una-vez** (clave `tag:umbral` en `player.mods.conjuntos`) y emite `conjunto_activado` → flash "CONJUNTO: nombre" + sfx + shake. **5 conjuntos**: Maestría del Compás (3 cadencia), Torrente de Tinta (3 lágrimas), Alma Incandescente (3 ignición), Viento del Reloj (3 movilidad), Autómata Pulido (4 stats). Verificado: node --check ×4, smoke **173/0** (+4 asserts: umbral de conjunto activa una vez, `aplicarEfectos` compone, pedestal de tesoro ≥2, catálogo `conjuntos` ≥5) y captura en-motor (tesoro con 2 pedestales; equipar 3 del mismo tag dispara el flash de conjunto). Hook temporal `__dbgGoto` retirado antes del commit | C2/C3 reutilizan andamiaje existente (rollItem, formato de efectos, patrón de sinergias) → cero mecánica inédita, solo composición. El conjunto se detecta al EQUIPAR (no al perder items; el bus no tiene `off`, así que no se "desactiva" — aceptable, los conjuntos son acumulativos). Falta de Fase C: C4 (Heat/pactos, la crítica de endgame), C1 (ampliar a ~50-60 reliquias, ya en 30), C5 (6 sellos + selector + sumidero de Memoria) | C4 — sistema Heat/pactos (condiciones apilables + Memoria por tramo), la pieza de endgame de mayor valor |
| 2026-07-19 | **FASE C — C4 SISTEMA HEAT / PACTOS** (Daniel: "Fase c"; la pieza crítica de endgame, carencia #6). Generaliza el viejo booleano **Cuerda Tensa** del Santuario a un catálogo data-driven de **pactos** (`data/pactos.json`): condiciones de dificultad OPT-IN, **apilables y con niveles**, donde cada tramo sube UN lever concreto y paga **+Memoria**. **6 pactos**: Cuerda Tensa (+hp enemigo), Furia del Reloj (+daño), Enjambre (+densidad de sala), Cacería (+velocidad), Corte de Élites (+prob de élite), Penuria (−corazones/oro). El **Heat total** = suma de niveles; la Memoria se escala por `1 + Σ(memoria_por_nivel × nivel)`. **Módulo `js/pactos.js`** (espejo de items/eventos): funciones **PURAS testeables** (`calcHeat/calcMemoriaMult/calcEfecto/nivelClamp`) + lectores de conveniencia `Pactos.*` que leen `GameState.pactos`+`DataDB.pactos` EN EL PUNTO DE USO (F5). **Efectos cableados** sin números mágicos: rooms `_rollSpawns`/`buildTower` (hp/densidad/élite/daño/velocidad, `velMult` nuevo consumido en el constructor de Enemy igual que `danoMult`), main `gainMemoria` (×memoriaMult) y drop de corazón/oro (÷Penuria). **Selector** = nuevo modo `'pactos'` que abre el nodo `cuarto_de_la_penumbra` del Santuario (antes toggle): lista navegable por toque/mando/teclado con icono procedural (`drawItemIcon`), −/+ steppers, pips de nivel, cabecera "Heat N · Memoria ×M.MM"; layout **adaptable a VH** (6 pactos + Volver caben en horizontal y en vertical). **Persistencia**: `SaveManager` sube a **schema 3** (acepta 1/2/3) con **migración defensiva** (`cuerdaTensa:true` → `{vida_tensa:2}`). Verificado: node --check ×6, smoke **183/0** (+10 asserts: pactos.json válido, calcHeat/MemoriaMult/Efecto/nivelClamp puros, Pactos lee estado, velMult escala velocidad), captura en-motor del selector (Heat 4 · Memoria ×1.66, iconos/pips/bordes por pacto, 0 errores) y bundle arranca con el módulo `pactos`. **BONUS**: `data/eventos.json` faltaba en la lista de datos del bundle (bug latente: los eventos no cargaban en el single-file) → añadido junto a `pactos.json`. Hook temporal `__dbgPactos` retirado antes del commit | Reusa el andamio de Cuerda Tensa como pedía el ROADMAP: cero mecánica inédita, solo generalización + composición de levers ya existentes. Los pactos se leen en el punto de uso → **todo tunable por F5** (números en pactos.json). El pacto se aplica a la PRÓXIMA run (elegido en el Santuario antes de descender); el Heat no se "desactiva" a mitad. Añadir un pacto = 1 entrada JSON si reusa un `efecto.tipo` ya cableado. Falta de Fase C: C1 (ampliar a ~50-60 reliquias, ya en 30) y C5 (6 sellos + selector + rangos de Santuario + sumidero de Memoria) | Daniel prueba el endgame: subir Heat en el Santuario y notar más dificultad + más Memoria. Siguiente: C1 (más reliquias) o C5 (sellos + sumidero), o feedback de game feel |
## Deuda técnica / notas
- `main.js` ~3100 líneas (god-file): trocear en R4; hasta entonces, cambios quirúrgicos.
- Colisión de nombre "sello" (Esfera / Santuario / elementales): aclarar en R2.4.
- Hooks `window.__mistveil` expuestos (retirar en R4.3/R5).
